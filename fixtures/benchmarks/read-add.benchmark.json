{
  "id": "read-add",
  "name": "Read source file",
  "description": "Agent reads Add.java via fs.read and completes in ≤2 steps.",
  "workspace": "fixtures/repos/sample-lib",
  "model": "fixtures/models/read-add.jsonl",
  "mode": "agent",
  "user_message": "read Add.java",
  "max_steps": 10,
  "expect": {
    "status": "succeeded",
    "max_tool_calls": 1,
    "max_step_events": 2,
    "event_types_contains": ["tool.completed", "run.completed"],
    "forbid_event_types": ["run.failed", "tool.failed"]
  }
}
