{
  "id": "symbols-find",
  "name": "Symbol search",
  "description": "Find Add type via symbols.search then read file.",
  "workspace": "fixtures/repos/sample-lib",
  "model": "fixtures/models/symbols-find.jsonl",
  "mode": "agent",
  "user_message": "find Add class and read it",
  "max_steps": 12,
  "expect": {
    "status": "succeeded",
    "max_tool_calls": 2,
    "max_step_events": 3,
    "event_types_contains": ["tool.completed", "run.completed"],
    "forbid_event_types": ["run.failed"]
  }
}
